---
name: latex-math
description: >
  استخدم هذه المهارة في أي وقت يحتاج فيه المستخدم لكتابة أو عرض معادلات رياضية أو محاسبية أو إحصائية
  أو أي رموز علمية. يشمل ذلك: المعادلات المحاسبية، تحليل التكلفة والربح، المعادلات الجبرية،
  الكسور، النسب المئوية، الجذور، المتجهات، المصفوفات، والرموز الرياضية.
  استخدم هذه المهارة دائماً عند ظهور كلمات مثل: معادلة، حساب، كسر، ناتج، نقطة التعادل،
  هامش المساهمة، نسبة، جذر، أو أي طلب لعرض صيغة رياضية بشكل واضح وجميل.
---

# مهارة: كتابة المعادلات الرياضية بـ LaTeX

## الهدف

توليد معادلات رياضية جميلة ومنظمة باستخدام صيغة LaTeX/KaTeX القابلة للعرض في التطبيقات والمواقع.

---

## متى تستخدم هذه المهارة؟

- عند طلب كتابة معادلة أو صيغة رياضية
- عند عرض خطوات حل مسألة محاسبية أو رياضية أو إحصائية
- عند إنشاء قوائم دخل أو تقارير مالية تحتوي على حسابات
- عند شرح أي مفهوم يحتاج رموزاً رياضية

---

## قواعد الكتابة

### 1. أنواع العرض

**معادلة منفردة في سطر مستقل** (Block):

```
$$  المعادلة هنا  $$
```

**معادلة داخل الجملة** (Inline):

```
$ المعادلة هنا $
```

---

### 2. الأوامر الأساسية

| الغرض            | الأمر              | مثال الكود          | النتيجة   |
| ---------------- | ------------------ | ------------------- | --------- |
| كسر              | `\frac{بسط}{مقام}` | `\frac{40500}{1.5}` | 40500/1.5 |
| ضرب              | `\times`           | `27000 \times 5`    | 27000 × 5 |
| تقسيم            | `\div`             | `a \div b`          | a ÷ b     |
| جذر تربيعي       | `\sqrt{x}`         | `\sqrt{144}`        | √144      |
| جذر n            | `\sqrt[n]{x}`      | `\sqrt[3]{27}`      | ∛27       |
| رفع للأس         | `^{}`              | `x^{2}`             | x²        |
| مؤشر سفلي        | `_{}`              | `x_{1}`             | x₁        |
| إطار/تظليل       | `\boxed{}`         | `\boxed{27000}`     | [27000]   |
| نص عادي          | `\text{}`          | `\text{وحدة}`       | وحدة      |
| يساوي تقريباً    | `\approx`          | `x \approx 3.14`    | x ≈ 3.14  |
| أكبر من أو يساوي | `\geq`             | `x \geq 0`          | x ≥ 0     |
| أصغر من أو يساوي | `\leq`             | `x \leq 100`        | x ≤ 100   |
| مجموع            | `\sum`             | `\sum_{i=1}^{n}`    | Σ         |
| لا نهاية         | `\infty`           | `x \to \infty`      | x → ∞     |
| فاصل آلاف        | `{,}`              | `27{,}000`          | 27,000    |

---

### 3. أنماط شائعة في المسائل المحاسبية

**نقطة التعادل بالوحدات:**

```latex
$$\text{بالوحدات} = \frac{\text{إجمالي التكاليف الثابتة}}{\text{هامش المساهمة للوحدة}} = \frac{40{,}500}{1.5} = \boxed{27{,}000 \text{ وحدة}}$$
```

**نقطة التعادل بالقيمة:**

```latex
$$\text{بالقيمة} = 27{,}000 \times 5 = \boxed{135{,}000 \text{ جنيه}}$$
```

**نسبة مئوية:**

```latex
$$\text{نسبة هامش المساهمة} = \frac{54{,}000}{180{,}000} \times 100 = \boxed{30\%}$$
```

**معادلة المبيعات المستهدفة:**

```latex
$$\text{المبيعات المطلوبة} = \frac{\text{التكاليف الثابتة} + \text{الربح المستهدف}}{\text{نسبة هامش المساهمة}} = \frac{40{,}500 + 18{,}000}{30\%} = \boxed{195{,}000 \text{ جنيه}}$$
```

---

### 4. جداول مع معادلات

لعرض جدول بيانات مالية مع حسابات، استخدم Markdown عادي للجدول وضع المعادلات داخله:

```markdown
| البيان        | الحساب                   | النتيجة                           |
| ------------- | ------------------------ | --------------------------------- |
| هامش المساهمة | $$5 - 3.5$$              | $$\boxed{1.5 \text{ جنيه}}$$      |
| نقطة التعادل  | $$\frac{40{,}500}{1.5}$$ | $$\boxed{27{,}000 \text{ وحدة}}$$ |
```

---

### 5. تنسيق الأرقام العربية

- استخدم دائماً `{,}` للفاصلة في الآلاف: `27{,}000` وليس `27,000`
- للأرقام العشرية: `3.14` (النقطة وليس الفاصلة)
- للنص العربي داخل المعادلة: استخدم `\text{النص هنا}`

---

## مثال كامل — حل مسألة محاسبية

عند طلب حل مسألة تحليل التكلفة والحجم والربح، اتبع الخطوات التالية:

### الخطوة 1: استخراج البيانات

```
سعر البيع = 5 جنيه
التكاليف المتغيرة للوحدة = 3.5 جنيه
إجمالي التكاليف الثابتة = 40,500 جنيه
```

### الخطوة 2: كتابة المعادلات بـ LaTeX

```latex
$$\text{هامش المساهمة للوحدة} = 5 - 3.5 = \boxed{1.5 \text{ جنيه}}$$

$$\text{نقطة التعادل} = \frac{40{,}500}{1.5} = \boxed{27{,}000 \text{ وحدة}}$$

$$\text{نقطة التعادل بالقيمة} = 27{,}000 \times 5 = \boxed{135{,}000 \text{ جنيه}}$$
```

---

## ملاحظات للمطورين

### كيفية تفعيل عرض LaTeX في تطبيقك:

**باستخدام KaTeX (أسرع):**

```html

```

**باستخدام MathJax (أكثر توافقاً):**

```html
MathJax = { tex: { inlineMath: [['$', '$'], ['\\(', '\\)']] } };
```

**في React مع react-katex:**

```bash
npm install react-katex katex
```

```jsx
import { InlineMath, BlockMath } from "react-katex";
import "katex/dist/katex.min.css";

// معادلة في سطر

// معادلة في سطر مستقل
```

---

## الإخراج المتوقع

عند استخدام هذه المهارة، يجب أن يكون الناتج:

1. ✅ معادلات واضحة وجميلة قابلة للعرض
2. ✅ نص تفسيري بالعربية حول كل معادلة
3. ✅ خطوات منطقية متسلسلة
4. ✅ الإجابة النهائية داخل `\boxed{}` للتمييز
5. ✅ وحدات القياس داخل `\text{}` بعد كل رقم
